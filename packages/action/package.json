{
  "name": "meridian",
  "version": "0.1.0",
  "description": "Replay every prompt change against what already worked.",
  "type": "module",
  "bin": { "meridian": "./lib/cli.js" },
  "scripts": { "compile": "tsc", "build": "npm run compile && ncc build lib/index.js --minify --out dist", "test": "node --test test/*.test.js" },
  "dependencies": { "@actions/core": "^1.10.0", "@octokit/rest": "^21.0.0", "commander": "^12.0.0" },
  "devDependencies": { "@types/node": "^20.0.0", "typescript": "^5.4.0", "@vercel/ncc": "^0.38.3" }
}
