import { motion } from 'motion/react'
/** Renders the full-width product statement with word reveals. */
export default function Statement() { return <section className="py-24 md:py-32 flex items-center bg-[var(--bg-primary)]"><motion.p initial="hidden" whileInView="visible" viewport={{ once:false, amount:.3 }} variants={{ hidden:{}, visible:{ transition:{ staggerChildren:.05 } } }} className="w-full px-6 md:px-8 font-display text-[clamp(2rem,6vw,4.5rem)] leading-[0.95] tracking-tight text-center max-w-4xl mx-auto">{'A prompt that passed once is not a prompt that always will.'.split(' ').map((word) => <motion.span key={word} variants={{ hidden:{ opacity:0, filter:'blur(6px)' }, visible:{ opacity:1, filter:'blur(0px)' } }} className="inline-block mr-[0.25em]">{word}</motion.span>)}</motion.p></section> }
