import { motion } from 'motion/react'
const layers = [['01','Golden traces stored','Every confirmed-good run of your agent is captured as a trace in SigNoz and marked as a baseline.'],['02','Replay on push','When a prompt changes, Meridian re-runs the same inputs against the new version and records a fresh trace.'],['03','Diff posted to the PR','Tool call counts, latency and token cost are compared against the baseline, and the verdict lands as a comment before anyone merges.']]
/** Renders the three-step regression mechanism. */
export default function Mechanism() { return <section className="py-24 md:py-32 bg-[var(--bg-primary)]"><div className="max-w-4xl mx-auto px-6 md:px-8"><motion.h2 initial={{ opacity:0, filter:'blur(8px)', y:20 }} whileInView={{ opacity:1, filter:'blur(0px)', y:0 }} viewport={{ once:false, amount:.1 }} transition={{ duration:.6, ease:[.16,1,.3,1] }} className="font-display text-3xl md:text-4xl font-semibold tracking-tight">How Meridian catches a regression</motion.h2><div className="mt-16 flex flex-col gap-4">{layers.map(([number,title,description], index) => <motion.div key={number} initial={{ opacity:0, y:20 }} whileInView={{ opacity:1, y:0 }} viewport={{ once:false, amount:.1 }} transition={{ duration:.6, ease:[.16,1,.3,1], delay:index*.15 }} className="border border-[color:var(--border-default)] rounded-[var(--radius-lg)] p-6 flex items-start gap-4 hover:border-[var(--accent-dim)] transition-colors"><span className="font-mono text-sm text-[var(--text-muted)] min-w-[2rem]">{number}</span><div><h3 className="font-display text-base font-medium">{title}</h3><p className="text-sm text-[var(--text-secondary)] mt-1 leading-relaxed">{description}</p></div></motion.div>)}</div></div></section> }
