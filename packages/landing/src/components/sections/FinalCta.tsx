import { motion } from 'motion/react'
import { ArrowUpRight } from 'lucide-react'
/** Renders the final install call to action and footer. */
export default function FinalCta() { return <section className="py-24 md:py-32 bg-[var(--bg-secondary)] border-t border-[color:var(--border-subtle)]"><motion.div initial={{ opacity:0, filter:'blur(8px)', y:20 }} whileInView={{ opacity:1, filter:'blur(0px)', y:0 }} viewport={{ once:false, amount:.1 }} transition={{ duration:.6, ease:[.16,1,.3,1] }} className="max-w-3xl mx-auto px-6 md:px-8 text-center"><h2 className="font-display text-3xl md:text-4xl font-semibold tracking-tight">Install Meridian before your next prompt ships.</h2><div className="mt-8 flex items-center justify-center gap-4"><a href="https://github.com" className="group bg-[var(--accent)] text-[var(--bg-primary)] text-sm font-medium pl-5 pr-1.5 py-1.5 rounded-[var(--radius-pill)] flex items-center gap-3 hover:bg-[var(--accent-hover)] transition-colors">Install Meridian <span className="w-7 h-7 rounded-full bg-black/10 flex items-center justify-center"><ArrowUpRight size={14} /></span></a><a href="https://github.com" className="text-[var(--text-secondary)] text-sm font-medium hover:text-[var(--text-primary)] transition-colors">View on GitHub</a></div><footer className="mt-20 pt-8 border-t border-[color:var(--border-subtle)] flex flex-col md:flex-row items-center justify-between gap-4"><span className="font-display text-sm text-[var(--text-secondary)]">{/* Logo slot: replace with public/logo.svg once provided */}Meridian</span><span className="font-mono text-xs text-[var(--text-muted)] tracking-[0.05em]">built for agents of signoz</span></footer></motion.div></section> }
